1. 存储基于磁盘，按页的方式进行管理。
2. 缓冲池
  1. 读取：会先读取缓冲池，若缓冲池命中则直接返回，否则读取磁盘，并存储在缓冲池。
  2. 修改：先修改缓冲池中的页，再已一定的频率刷新到磁盘，通过CheckPoint机制刷新到磁盘。
  3. 缓冲池大小是可设定的
  4. 缓冲池中存储内容：
    - 索引页
    - 数据页
    - undo页
    - 插入缓冲（insert buffer）
    - 自适应哈希索引
    - InnoDB存储的锁信息
    - 数据字典
